name: QA Manual Test

on:
  schedule:
    - cron: "*/5 * * * *"  # every 5 minutes
  workflow_dispatch:        # also allow manual trigger

jobs:
  test-qa:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout QA branch
        uses: actions/checkout@v4
        with:
          ref: qa

      - name: QA Test Step
        run: echo "QA workflow running successfully!"
